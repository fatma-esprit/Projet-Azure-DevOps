trigger:
- main

pool:
  name: Default  # Ton agent auto-hébergé

steps:
- task: NodeTool@0
  inputs:
    versionSpec: '12.x'  # Utilisation de Node.js 12 compatible avec Angular 6
  displayName: 'Installer Node.js 12'

- script: |
    echo "🔍 Vérification du répertoire de travail"
    cd
    dir
  workingDirectory: $(Build.SourcesDirectory)
  displayName: 'Afficher le répertoire et fichiers (Windows)'

- script: |
    echo "♻️ Nettoyage du cache et des dépendances"
    rmdir /s /q node_modules
    del package-lock.json
    npm cache clean --force
    npm ci --legacy-peer-deps
    npm rebuild node-sass
  workingDirectory: $(Build.SourcesDirectory)
  displayName: 'Installer les dépendances'

- script: |
    echo "🚀 Installation de Angular CLI"
    npm install -g @angular/cli@6.0.8 --legacy-peer-deps --force
    ng version
  displayName: 'Installer Angular CLI'

- script: |
    echo "🔧 Installation de @angular-devkit/build-angular"
    npm install --save-dev @angular-devkit/build-angular
  displayName: 'Installer @angular-devkit/build-angular'

- script: |
    echo "🏗️ Lancement du build Angular"
    ng build --configuration=production
  workingDirectory: $(Build.SourcesDirectory)
  displayName: 'Construire l’application Angular'

- task: PublishBuildArtifacts@1
  inputs:
    pathToPublish: '$(Build.SourcesDirectory)/dist'
    artifactName: 'drop'
  displayName: 'Publier les artefacts de build'
